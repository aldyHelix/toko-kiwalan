includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 6
    paths:
        - app
        - database
        - routes
        - config
    # Raise the level incrementally as the codebase matures (target: 8).
